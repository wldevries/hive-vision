# The capture/label store (photos, labels.jsonl). Anchored to the repo root so it
# does NOT also match the hivevision/data/ source package.
/data/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# uv / virtualenv
.venv/

# Model weights — trained and downloaded pretrained (e.g. yolo11n-pose.pt lands in the
# CWD) — plus experiment outputs. No checkpoint belongs in the repo.
*.pt
*.pth
*.onnx
runs/

# Local environment config
.env

# OS / editor
.DS_Store
*.swp
